const form = document.getElementById('myform')
form.addEventListener('submit', (e)=>{
    e.preventDefault();
    // console.log(e.target.state)
    // console.log()
    let state = form.elements['commodity'].value
    let 
})